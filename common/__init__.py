"""Shared library for the Information Mediator building block.

This package is imported by every independent server (CA, TSA, Global,
Security Server, Application). Servers are still independent processes /
folders with their own DB and config; `common` is just a code library
(like X-Road's shared Java libs), not a running server.
"""
